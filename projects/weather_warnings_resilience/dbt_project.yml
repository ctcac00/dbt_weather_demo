name: 'weather_warnings_resilience'
version: '1.0.0'
config-version: 2

profile: 'weather'

dbt-cloud:
  project-id: 18518
  defer-env-id: 70569
  account_id: 664
  account-host: sa318.eu1.dbt.com

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  weather_warnings_resilience:
    +persist_docs:
      relation: true
      columns: true
    reporting:
      +materialized: view
      +schema: reporting
    reports:
      +materialized: table
      +schema: reports
