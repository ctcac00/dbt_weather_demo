version: 2

models:
  - name: metricflow_time_spine
    description: "Daily time spine for MetricFlow semantic layer queries."
    time_spine:
      standard_granularity_column: date_day
    columns:
      - name: date_day
        description: "Daily date spine for time-based semantic aggregations."
        granularity: day
