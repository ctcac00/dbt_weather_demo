name: 'weather_climate_data_services'
version: '1.0.0'
config-version: 2

profile: 'weather'

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  weather_climate_data_services:
    +persist_docs:
      relation: true
      columns: true
    reporting:
      +materialized: view
      +schema: reporting
    reports:
      +materialized: table
      +schema: reports
